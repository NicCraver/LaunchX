## 1. 代码调查和问题定位

- [x] 1.1 定位简约模式下扩展界面的视图控制器代码
- [x] 1.2 识别扩展内容容器视图和相关约束
- [x] 1.3 分析简约模式和全屏模式的视图层级差异
- [x] 1.4 确认导致扩展内容被挤压的具体约束冲突

## 2. 约束修复实现

- [x] 2.1 在扩展界面显示时检测当前显示模式（简约/全屏）
- [x] 2.2 为简约模式下的扩展容器视图调整约束优先级
- [x] 2.3 移除或禁用导致内容被限制在输入框内的冲突约束
- [x] 2.4 确保扩展内容视图可以正确展开到完整可用空间

## 3. 模式切换适配

- [x] 3.1 在模式切换时正确更新扩展容器的约束
- [x] 3.2 添加约束更新的动画过渡，避免布局闪烁
- [x] 3.3 确保扩展的滚动位置和选择状态在模式切换时保持

## 4. 测试验证

- [ ] 4.1 测试简约模式下打开 Zed 扩展，验证内容正确展开
- [ ] 4.2 测试简约模式下打开 VSCode 等其他扩展，验证布局正确
- [ ] 4.3 测试全屏模式下的扩展显示，确认无回归问题
- [ ] 4.4 测试简约模式和全屏模式之间切换时扩展的显示行为
- [ ] 4.5 测试不同内容高度的扩展，验证自适应布局

## 修复总结

已完成核心修复，修改了 `SearchPanelViewController+Search.swift` 中的 `updateVisibility()` 函数：

1. 在 `isExpanded` 计算中添加了 `|| isInAnyExtensionMode` 检查
2. 在窗口高度更新逻辑中添加了 `|| isInAnyExtensionMode` 条件
3. 确保所有扩展模式（IDE、文件夹、网页直达、实用工具、书签、2FA）在简约模式下都能正确展开到 500px

修改位置：`LaunchX/Views/Search/SearchPanelViewController+Search.swift:265-277`

构建状态：✅ 编译成功，无错误
